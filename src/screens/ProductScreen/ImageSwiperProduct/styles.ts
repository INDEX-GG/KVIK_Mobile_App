import {StyleSheet} from "react-native";

const ImageSwiperProductStyle = () => {
    return StyleSheet.create({
        container: {
            height: 280,
        }
    })
}

export const useImageSwiperProductStyle = () => ImageSwiperProductStyle();
