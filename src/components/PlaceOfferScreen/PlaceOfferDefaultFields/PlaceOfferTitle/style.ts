import { StyleSheet } from 'react-native';

const PlaceOfferTitleStyles = () => {
  return StyleSheet.create({
    container: {},
  });
};

export const usePlaceOfferTitleStyles = () => PlaceOfferTitleStyles();
