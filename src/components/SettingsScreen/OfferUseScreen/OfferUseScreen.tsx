import React from 'react';
import { View } from 'react-native';
import RobotoText from '../../../UI/RobotoText';

const OfferUseScreen = () => {
  return (
    <View>
      <RobotoText weight="b">OfferUseScreen</RobotoText>
    </View>
  );
};

export default React.memo(OfferUseScreen);
