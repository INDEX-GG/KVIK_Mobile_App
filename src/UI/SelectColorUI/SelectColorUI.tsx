import React from 'react';
import { View, Text } from 'react-native';

const SelectColorUI = () => {
  return (
    <View>
      <Text>1</Text>
    </View>
  );
};

export default React.memo(SelectColorUI);
